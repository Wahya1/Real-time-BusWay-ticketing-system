package ma.fstm.ilisi.service;
/*
import com.itextpdf.io.image.ImageDataFactory;
import com.itextpdf.kernel.pdf.PdfDocument;
import com.itextpdf.kernel.pdf.PdfWriter;
import com.itextpdf.layout.Document;
import com.itextpdf.layout.element.Image;
import com.itextpdf.layout.element.Paragraph;*/
/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */

     public class TicketPDFEExporter {
        /*
         public  static void exportToPDF(String ticketInfo, String qrCodeImagePath, String filePath) throws IOException {
            // Créer un nouveau document PDF
            PdfDocument pdfDoc = new PdfDocument(new PdfWriter(filePath));
            Document doc = new Document(pdfDoc);

            // Ajouter les informations du ticket (passager) au document PDF
            doc.add(new Paragraph(ticketInfo));

            // Ajouter le QR code au document PDF// Ajouter le QR code au document PDF
            Image qrCodeImage = new Image(ImageDataFactory.create(qrCodeImagePath));
            doc.add(qrCodeImage);

            doc.add(qrCodeImage);

            // Fermer le document
            doc.close();
        }*/
    }
